---
title: "本机授权"
description: "文件、终端、浏览器和桌面控制的本机权限"
---

# 本机授权

FoxWork 读取本机文件夹、运行终端命令或控制桌面前，必须取得你在当前 Mac 上的授权。公司服务器、远程工作区、MCP 和工作流不能绕过这一步。

文件访问失败时，在“设置 > 权限”中添加需要使用的文件夹。只授权任务需要的范围，不要为方便而开放整个磁盘。

桌面控制需要 macOS 的辅助功能和屏幕录制权限。摄像头、麦克风和蓝牙只在你主动使用对应功能时申请。撤销系统权限后，相关工具应立即停止工作。
